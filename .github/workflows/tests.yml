name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      VERSION: '3.3.1'
      SERVERPOD_PASSWORD_database: 'DaBA2ec_HKHwJnXQhg9zBSVTOKmaPptV'
      SERVERPOD_PASSWORD_redis: 'Fc2mjQpnvJ60bcN07tSiWhRAQKMy9ZCj'

    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'

      - name: Start containers
        working-directory: magic_recipe_server
        run: docker compose up --build -d

      - name: Install dependencies
        working-directory: magic_recipe_server
        run: dart pub get

      - name: Install Serverpod CLI
        working-directory: magic_recipe_server
        run: dart pub global activate serverpod_cli $VERSION

      - name: Run Serverpod generator
        working-directory: magic_recipe_server
        run: serverpod generate

      - name: Run tests
        working-directory: magic_recipe_server
        run: dart test

      - name: Stop containers
        if: always()
        working-directory: magic_recipe_server
        run: docker compose down -v
